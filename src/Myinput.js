// import React from 'react';
// import {useState} from 'react'
// import {mycontext} from './Context.js'
// import Change from './Change.js'
// import Result from './Result.js'

// const Myinputt = ({children}) => 
// function Myinputt({children})  {
//    const [items, setItems] = useState([])
//     const addtocart = (Img, Name, Price) => {
//     setItems((prevState) => [...prevState, {Img, Name, Price}]);  
    // The code above means we need to push whatever we had on the previous to the name and value object. 
  // } 

 

  //  name is going to be our state then we use the add to cart to make it work. 
  // The add to cart is basically our general funcitonality that wil work everywhere provided we push it to our component. 
 


  // we want name, value and then we want.. 
  //  What states do we set here for God sakes? Lets do
  // we will call children
  //  const [condition, setCondition] = useState(true)
  //  const box = ([])

//   return (
//     <> 
 
//     <mycontext.Provider value={{items, addtocart}}> 
//     {children}
//     </mycontext.Provider>
//     </>
//   )
// }

// export default Myinputt